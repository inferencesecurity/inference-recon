# Copy to .env and adjust as needed.
# .env is gitignored ??? never commit credentials.

# Anthropic API key ??? required for eval/batch_scan.py
# Get yours at https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...

POSTGRES_DB=ai_sec_review
POSTGRES_USER=eval
POSTGRES_PASSWORD=eval

DB_PORT=5432
ADMINER_PORT=8080
METABASE_PORT=3000

# Used by eval/ingest.py ??? point at Docker Postgres or any Postgres instance.
DATABASE_URL=postgresql://eval:eval@localhost:5432/ai_sec_review
