# Cloned repos — generated at scan time, not tracked
repos/

# Generated findings JSON — stored in the DB, not tracked
# Raw response dumps (from failed JSON parses) also land here
findings/

# Corpus target list files — local only, never commit
# (may reference private repos or internal project names)
*.txt
