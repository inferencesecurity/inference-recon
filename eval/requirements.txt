# eval pipeline dependencies
# Install: pip install -r eval/requirements.txt

# Provider SDKs — official packages only, no intermediary libraries
anthropic>=0.40.0
openai>=1.50.0
google-generativeai>=0.8.0

# Git repo management
gitpython>=3.1.0

# Database
psycopg2-binary>=2.9.0

# HTTP + env (sync_pricing.py, batch_scan.py)
certifi>=2024.0
python-dotenv>=1.0

# Web UI
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.0
python-multipart>=0.0.9
